# AI Red Teamer — curated dependencies (Python 3.11)
# Versions pinned to the tested environment. Only the packages actually
# imported by the notebooks/scripts are listed here.

# --- Core scientific / data stack ---
numpy==1.26.4
pandas==2.3.3
scikit-learn==1.3.1
matplotlib==3.10.8
seaborn==0.13.2
Pillow==9.4.0

# --- Deep learning ---
torch==2.9.1
torchvision==0.24.1
opacus                 # differential privacy (module 11 · DP-SGD); resolver picks a torch-compatible build

# --- NLP / transformers (spam filter, LLM guardrails) ---
nltk==3.9.2
transformers==4.57.3
safetensors==0.7.0

# --- Tooling / utilities ---
tqdm==4.67.1
joblib==1.5.3
requests==2.32.5
pydantic==2.12.5
split-folders          # malware image train/test split (module 02)
fastmcp==2.14.4        # Model Context Protocol client/server (module 07)

# --- HTB Academy helper library (installed from git) ---
git+https://github.com/PandaSt0rm/htb-ai-library
